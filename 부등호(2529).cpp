#include<iostream>
#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <algorithm>
using namespace std;

int main() {
	char s[10];
	int num[10];
	int N;
	scanf("%d", &N);
	for (int i = 0; i < N; i++)num[i] = i;
	for (int i = 0; i < N; i++)scanf("%c", s[i]);

	for (int i = 0; i < N; i++) {
		for (int j = 1; j < N; j++) {
			if (s[i] == '>') {

			}
		}
	}
	{

	}

	return 0;
}